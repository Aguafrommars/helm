apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "private.fullname" . }}-test-connection"
  labels:
    {{- include "private.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "private.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
