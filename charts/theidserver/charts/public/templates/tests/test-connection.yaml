apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "public.fullname" . }}-test-connection"
  labels:
    {{- include "public.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "public.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
