apiVersion: v1
kind: Service
metadata:
  name: {{ include "public.fullname" . }}
  labels:
    {{- include "public.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "public.selectorLabels" . | nindent 4 }}
